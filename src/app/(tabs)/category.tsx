import { Text, View } from "react-native";

export default function Category() {
	return (
		<View className="flex-1 items-center justify-center bg-slate-100">
			<Text className="text-2xl font-semibold text-red-900">
				Kategori sayfası
			</Text>
		</View>
	);
}
